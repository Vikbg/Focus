[package]
name = "focusd"
version = "0.0.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
focus-core = { path = "../../crates/focus-core" }
focus-linux = { path = "../../platform/linux" }
focus-platform = { path = "../../crates/focus-platform" }
focus-protocol = { path = "../../crates/focus-protocol" }
focus-storage = { path = "../../crates/focus-storage" }
nix = { version = "0.31.3", features = ["fs", "socket", "user"] }
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread", "signal", "sync", "time"] }

[dev-dependencies]
focusctl = { path = "../focusctl" }

[lints]
workspace = true
