name: Inline Task 4 Coordinator

on:
  push:
    branches:
      - agent/audit-remediation-inline

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/audit-remediation-inline
          fetch-depth: 0
      - name: Apply coordinator hardening
        run: |
          python3 .inline-task4-coordinator.py
          cargo fmt --all
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bins/focusd/src/lib.rs
          git commit -m "fix: make arming compensation idempotent and reportable"
          git push origin HEAD:agent/audit-remediation-inline
